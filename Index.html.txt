<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nethum Photography | Capturing Moments, Creating Memories</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;0,800;0,900;1,400;1,500;1,600;1,700;1,800;1,900&family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --gold: #D4AF37;
            --gold-light: #F4E4A6;
            --gold-dark: #B8960C;
            --black: #0a0a0a;
            --black-light: #1a1a1a;
            --white: #ffffff;
            --white-off: #f5f5f5;
            --gray: #888888;
            --gray-light: #cccccc;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--black);
            color: var(--white);
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
        }

        /* Scroll Animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .fade-in-left {
            opacity: 0;
            transform: translateX(-50px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .fade-in-left.visible {
            opacity: 1;
            transform: translateX(0);
        }

        .fade-in-right {
            opacity: 0;
            transform: translateX(50px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .fade-in-right.visible {
            opacity: 1;
            transform: translateX(0);
        }

        .scale-in {
            opacity: 0;
            transform: scale(0.9);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .scale-in.visible {
            opacity: 1;
            transform: scale(1);
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 20px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            transition: all 0.4s ease;
            background: transparent;
        }

        nav.scrolled {
            background: rgba(10, 10, 10, 0.95);
            backdrop-filter: blur(10px);
            padding: 15px 5%;
            box-shadow: 0 2px 20px rgba(0,0,0,0.3);
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--gold);
            text-decoration: none;
            letter-spacing: 1px;
        }

        .logo span {
            color: var(--white);
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 40px;
        }

        .nav-links a {
            color: var(--white);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            letter-spacing: 0.5px;
            position: relative;
            transition: color 0.3s ease;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--gold);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: var(--gold);
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            color: var(--white);
            font-size: 1.5rem;
            cursor: pointer;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1516035069371-29a1b244cc32?w=1920&q=80') center/cover no-repeat;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(10,10,10,0.85) 0%, rgba(10,10,10,0.7) 50%, rgba(10,10,10,0.9) 100%);
        }

        .hero-content {
            position: relative;
            z-index: 2;
            text-align: center;
            padding: 0 20px;
            max-width: 900px;
        }

        .hero-badge {
            display: inline-block;
            padding: 8px 25px;
            border: 1px solid var(--gold);
            color: var(--gold);
            font-size: 0.85rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            margin-bottom: 30px;
            opacity: 0;
            animation: fadeInUp 1s ease 0.3s forwards;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 6vw, 5rem);
            font-weight: 700;
            margin-bottom: 15px;
            line-height: 1.1;
            opacity: 0;
            animation: fadeInUp 1s ease 0.5s forwards;
        }

        .hero h1 .gold-text {
            color: var(--gold);
        }

        .hero-subtitle {
            font-size: clamp(1rem, 2.5vw, 1.3rem);
            color: var(--gray-light);
            margin-bottom: 20px;
            font-weight: 300;
            letter-spacing: 1px;
            opacity: 0;
            animation: fadeInUp 1s ease 0.7s forwards;
        }

        .hero-services {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 30px;
            opacity: 0;
            animation: fadeInUp 1s ease 0.9s forwards;
        }

        .hero-services span {
            color: var(--gold-light);
            font-size: 0.9rem;
            letter-spacing: 1px;
        }

        .hero-services .dot {
            color: var(--gold);
        }

        .hero-contact {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-bottom: 40px;
            color: var(--white);
            font-size: 1rem;
            opacity: 0;
            animation: fadeInUp 1s ease 1.1s forwards;
        }

        .hero-contact i {
            color: var(--gold);
            font-size: 1.2rem;
        }

        .hero-contact a {
            color: var(--white);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .hero-contact a:hover {
            color: var(--gold);
        }

        .hero-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
            opacity: 0;
            animation: fadeInUp 1s ease 1.3s forwards;
        }

        .btn {
            padding: 14px 35px;
            border-radius: 0;
            font-size: 0.95rem;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.4s ease;
            cursor: pointer;
            border: none;
        }

        .btn-primary {
            background: var(--gold);
            color: var(--black);
        }

        .btn-primary:hover {
            background: var(--gold-light);
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(212, 175, 55, 0.3);
        }

        .btn-outline {
            background: transparent;
            color: var(--white);
            border: 2px solid var(--white);
        }

        .btn-outline:hover {
            background: var(--white);
            color: var(--black);
            transform: translateY(-3px);
        }

        .btn-whatsapp {
            background: #25D366;
            color: var(--white);
        }

        .btn-whatsapp:hover {
            background: #128C7E;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(37, 211, 102, 0.3);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Section Styles */
        section {
            padding: 100px 5%;
        }

        .section-header {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-header h2 {
            font-size: clamp(2rem, 4vw, 3rem);
            color: var(--gold);
            margin-bottom: 15px;
            position: relative;
            display: inline-block;
        }

        .section-header h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 3px;
            background: var(--gold);
        }

        .section-header p {
            color: var(--gray);
            font-size: 1.1rem;
            margin-top: 25px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        /* About Section */
        .about {
            background: var(--black-light);
        }

        .about-content {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
        }

        .about-content p {
            font-size: 1.15rem;
            line-height: 1.9;
            color: var(--gray-light);
        }

        .about-content .lead {
            font-size: 1.4rem;
            color: var(--white);
            font-family: 'Playfair Display', serif;
            margin-bottom: 20px;
            font-style: italic;
        }

        /* Gallery Section */
        .gallery {
            background: var(--black);
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .gallery-item {
            position: relative;
            overflow: hidden;
            aspect-ratio: 3/4;
            cursor: pointer;
        }

        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s ease;
        }

        .gallery-item:hover img {
            transform: scale(1.1);
        }

        .gallery-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to top, rgba(10,10,10,0.9) 0%, transparent 60%);
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            padding: 30px;
            opacity: 0;
            transition: opacity 0.4s ease;
        }

        .gallery-item:hover .gallery-overlay {
            opacity: 1;
        }

        .gallery-overlay h3 {
            color: var(--gold);
            font-size: 1.3rem;
            margin-bottom: 5px;
        }

        .gallery-overlay p {
            color: var(--gray-light);
            font-size: 0.9rem;
        }

        .gallery-featured {
            grid-column: span 2;
            aspect-ratio: 16/9;
        }

        @media (max-width: 768px) {
            .gallery-featured {
                grid-column: span 1;
                aspect-ratio: 3/4;
            }
        }

        /* Services Section */
        .services {
            background: var(--black-light);
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .service-card {
            background: var(--black);
            padding: 40px 30px;
            text-align: center;
            border: 1px solid rgba(212, 175, 55, 0.1);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: var(--gold);
            transform: scaleX(0);
            transition: transform 0.4s ease;
        }

        .service-card:hover {
            border-color: rgba(212, 175, 55, 0.3);
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        }

        .service-card:hover::before {
            transform: scaleX(1);
        }

        .service-icon {
            font-size: 2.5rem;
            color: var(--gold);
            margin-bottom: 20px;
        }

        .service-card h3 {
            font-size: 1.3rem;
            color: var(--white);
            margin-bottom: 15px;
        }

        .service-card p {
            color: var(--gray);
            font-size: 0.95rem;
            line-height: 1.7;
        }

        /* Why Choose Us */
        .why-us {
            background: var(--black);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .feature-item {
            display: flex;
            align-items: flex-start;
            gap: 20px;
        }

        .feature-icon {
            min-width: 50px;
            height: 50px;
            background: rgba(212, 175, 55, 0.1);
            border: 1px solid var(--gold);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--gold);
            font-size: 1.2rem;
        }

        .feature-text h3 {
            color: var(--white);
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        .feature-text p {
            color: var(--gray);
            font-size: 0.95rem;
            line-height: 1.7;
        }

        /* Location & Contact */
        .contact-section {
            background: var(--black-light);
        }

        .contact-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            max-width: 1200px;
            margin: 0 auto;
            align-items: start;
        }

        .contact-info h3 {
            color: var(--gold);
            font-size: 1.5rem;
            margin-bottom: 20px;
        }

        .contact-info p {
            color: var(--gray-light);
            line-height: 1.8;
            margin-bottom: 15px;
        }

        .contact-info .address {
            font-size: 1.1rem;
            color: var(--white);
            margin-bottom: 30px;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 20px;
        }

        .contact-item i {
            width: 45px;
            height: 45px;
            background: rgba(212, 175, 55, 0.1);
            border: 1px solid var(--gold);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--gold);
            font-size: 1.1rem;
        }

        .contact-item div strong {
            color: var(--white);
            display: block;
            margin-bottom: 3px;
        }

        .contact-item div span, .contact-item div a {
            color: var(--gray);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .contact-item div a:hover {
            color: var(--gold);
        }

        .map-container {
            width: 100%;
            height: 400px;
            border: 1px solid rgba(212, 175, 55, 0.2);
            overflow: hidden;
        }

        .map-container iframe {
            width: 100%;
            height: 100%;
            border: none;
            filter: grayscale(100%) invert(90%);
        }

        @media (max-width: 768px) {
            .contact-grid {
                grid-template-columns: 1fr;
            }
        }

        /* CTA Section */
        .cta {
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.1) 0%, rgba(10,10,10,0.95) 100%);
            text-align: center;
            padding: 80px 5%;
        }

        .cta h2 {
            font-size: clamp(1.8rem, 4vw, 2.8rem);
            color: var(--white);
            margin-bottom: 20px;
        }

        .cta p {
            color: var(--gray);
            font-size: 1.1rem;
            margin-bottom: 30px;
        }

        /* Footer */
        footer {
            background: var(--black);
            border-top: 1px solid rgba(212, 175, 55, 0.1);
            padding: 60px 5% 30px;
            text-align: center;
        }

        .footer-logo {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            color: var(--gold);
            margin-bottom: 15px;
        }

        .footer-tagline {
            color: var(--gray);
            font-size: 1rem;
            letter-spacing: 2px;
            margin-bottom: 30px;
        }

        .footer-social {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
        }

        .footer-social a {
            width: 45px;
            height: 45px;
            border: 1px solid rgba(212, 175, 55, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--gray);
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .footer-social a:hover {
            background: var(--gold);
            color: var(--black);
            border-color: var(--gold);
        }

        .footer-bottom {
            border-top: 1px solid rgba(255,255,255,0.05);
            padding-top: 30px;
            color: var(--gray);
            font-size: 0.9rem;
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background: rgba(10, 10, 10, 0.98);
                flex-direction: column;
                padding: 30px;
                gap: 25px;
            }

            .nav-links.active {
                display: flex;
            }

            .mobile-menu-btn {
                display: block;
            }

            .hero-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 280px;
                justify-content: center;
            }

            section {
                padding: 60px 5%;
            }

            .gallery-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Loading Animation */
        .loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--black);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            transition: opacity 0.5s ease, visibility 0.5s ease;
        }

        .loader.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .loader-content {
            text-align: center;
        }

        .loader-content i {
            font-size: 3rem;
            color: var(--gold);
            animation: pulse 1.5s ease infinite;
        }

        .loader-content p {
            margin-top: 20px;
            color: var(--gray);
            font-size: 0.9rem;
            letter-spacing: 2px;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.1); opacity: 0.7; }
        }

        /* Scroll to top */
        .scroll-top {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 50px;
            height: 50px;
            background: var(--gold);
            color: var(--black);
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            z-index: 999;
        }

        .scroll-top.visible {
            opacity: 1;
            visibility: visible;
        }

        .scroll-top:hover {
            background: var(--gold-light);
            transform: translateY(-3px);
        }
    </style>
</head>
<body>
    <!-- Loader -->
    <div class="loader" id="loader">
        <div class="loader-content">
            <i class="fas fa-camera"></i>
            <p>NETHUM PHOTOGRAPHY</p>
        </div>
    </div>

    <!-- Scroll to Top -->
    <button class="scroll-top" id="scrollTop" onclick="scrollToTop()">
        <i class="fas fa-arrow-up"></i>
    </button>

    <!-- Navigation -->
    <nav id="navbar">
        <a href="#" class="logo">Nethum <span>Photography</span></a>
        <ul class="nav-links" id="navLinks">
            <li><a href="#home">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#gallery">Gallery</a></li>
            <li><a href="#services">Services</a></li>
            <li><a href="#why-us">Why Us</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
        <button class="mobile-menu-btn" id="mobileMenuBtn" onclick="toggleMenu()">
            <i class="fas fa-bars"></i>
        </button>
    </nav>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <div class="hero-badge">Professional Photography</div>
            <h1>Nethum <span class="gold-text">Photography</span></h1>
            <p class="hero-subtitle">Professional Photography Services</p>
            <div class="hero-services">
                <span>Weddings</span>
                <span class="dot">•</span>
                <span>Pre Shoots</span>
                <span class="dot">•</span>
                <span>Portraits</span>
                <span class="dot">•</span>
                <span>Events</span>
                <span class="dot">•</span>
                <span>Nature Photography</span>
            </div>
            <div class="hero-contact">
                <i class="fab fa-whatsapp"></i>
                <a href="https://wa.me/94773950975" target="_blank">+94 77 395 0975</a>
            </div>
            <div class="hero-buttons">
                <a href="#gallery" class="btn btn-primary">
                    <i class="fas fa-images"></i> View Gallery
                </a>
                <a href="https://wa.me/94773950975" target="_blank" class="btn btn-whatsapp">
                    <i class="fab fa-whatsapp"></i> WhatsApp Booking
                </a>
                <a href="#about" class="btn btn-outline">
                    <i class="fas fa-book-open"></i> About Us
                </a>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section class="about" id="about">
        <div class="section-header fade-in">
            <h2>Welcome to Nethum Photography</h2>
            <p>Where every moment becomes a masterpiece</p>
        </div>
        <div class="about-content">
            <p class="lead fade-in">"We believe every smile, every celebration, and every unforgettable moment deserves to be captured beautifully."</p>
            <p class="fade-in">With creativity, professional editing, and attention to detail, we transform ordinary moments into lifelong memories. Our passion for photography drives us to deliver exceptional quality that you'll treasure forever.</p>
        </div>
    </section>

    <!-- Gallery Section -->
    <section class="gallery" id="gallery">
        <div class="section-header fade-in">
            <h2>Our Gallery</h2>
            <p>A glimpse into our world of captured moments</p>
        </div>
        <div class="gallery-grid">
            <div class="gallery-item gallery-featured fade-in">
                <img src="https://images.unsplash.com/photo-1531746020798-e6953c6e8e04?w=800&q=80" alt="Portrait Photography">
                <div class="gallery-overlay">
                    <h3>Portrait Collection</h3>
                    <p>Black & White Patterned Shirt Portrait</p>
                </div>
            </div>
            <div class="gallery-item fade-in">
                <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=800&q=80" alt="Portrait in Maroon">
                <div class="gallery-overlay">
                    <h3>Classic Portrait</h3>
                    <p>Maroon & White Shirt Portrait</p>
                </div>
            </div>
            <div class="gallery-item fade-in">
                <img src="https://images.unsplash.com/photo-1519741497674-611481863552?w=800&q=80" alt="Wedding Photography">
                <div class="gallery-overlay">
                    <h3>Wedding Moments</h3>
                    <p>Capturing your special day</p>
                </div>
            </div>
            <div class="gallery-item fade-in">
                <img src="https://images.unsplash.com/photo-1465146344425-f00d5f5c8f07?w=800&q=80" alt="Nature Photography">
                <div class="gallery-overlay">
                    <h3>Nature's Beauty</h3>
                    <p>Stunning outdoor captures</p>
                </div>
            </div>
            <div class="gallery-item fade-in">
                <img src="https://images.unsplash.com/photo-1522673607200-164d1b6ce486?w=800&q=80" alt="Couple Photography">
                <div class="gallery-overlay">
                    <h3>Couple Sessions</h3>
                    <p>Romantic pre-wedding shoots</p>
                </div>
            </div>
            <div class="gallery-item fade-in">
                <img src="https://images.unsplash.com/photo-1511795409834-ef04bbd61622?w=800&q=80" alt="Event Photography">
                <div class="gallery-overlay">
                    <h3>Event Coverage</h3>
                    <p>Every celebration documented</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Services Section -->
    <section class="services" id="services">
        <div class="section-header fade-in">
            <h2>Our Services</h2>
            <p>Comprehensive photography solutions for every occasion</p>
        </div>
        <div class="services-grid">
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-ring"></i>
                </div>
                <h3>Wedding Photography</h3>
                <p>Complete coverage of your special day with artistic storytelling and emotional captures.</p>
            </div>
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-heart"></i>
                </div>
                <h3>Pre Wedding Shoots</h3>
                <p>Romantic sessions to celebrate your love story before the big day.</p>
            </div>
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-birthday-cake"></i>
                </div>
                <h3>Birthday Photography</h3>
                <p>Cherish every birthday milestone with professional, joyful captures.</p>
            </div>
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h3>Family Sessions</h3>
                <p>Beautiful family portraits that preserve precious moments together.</p>
            </div>
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-graduation-cap"></i>
                </div>
                <h3>Graduation Photography</h3>
                <p>Celebrate your academic achievements with memorable portraits.</p>
            </div>
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-mountain"></i>
                </div>
                <h3>Outdoor Photography</h3>
                <p>Stunning natural light portraits in beautiful outdoor locations.</p>
            </div>
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-user"></i>
                </div>
                <h3>Portrait Photography</h3>
                <p>Professional headshots and personal portraits that reflect your personality.</p>
            </div>
            <div class="service-card fade-in">
                <div class="service-icon">
                    <i class="fas fa-video"></i>
                </div>
                <h3>Cinematic Video Coverage</h3>
                <p>High-quality video production for events and special occasions.</p>
            </div>
        </div>
    </section>

    <!-- Why Choose Us -->
    <section class="why-us" id="why-us">
        <div class="section-header fade-in">
            <h2>Why Choose Nethum Photography?</h2>
            <p>What sets us apart from the rest</p>
        </div>
        <div class="features-grid">
            <div class="feature-item fade-in-left">
                <div class="feature-icon">
                    <i class="fas fa-check"></i>
                </div>
                <div class="feature-text">
                    <h3>Professional Quality</h3>
                    <p>High-end equipment and expert techniques ensure stunning results every time.</p>
                </div>
            </div>
            <div class="feature-item fade-in-right">
                <div class="feature-icon">
                    <i class="fas fa-magic"></i>
                </div>
                <div class="feature-text">
                    <h3>Creative Editing</h3>
                    <p>Artistic post-processing that enhances every image with a unique touch.</p>
                </div>
            </div>
            <div class="feature-item fade-in-left">
                <div class="feature-icon">
                    <i class="fas fa-tags"></i>
                </div>
                <div class="feature-text">
                    <h3>Affordable Packages</h3>
                    <p>Competitive pricing without compromising on quality or service.</p>
                </div>
            </div>
            <div class="feature-item fade-in-right">
                <div class="feature-icon">
                    <i class="fas fa-bolt"></i>
                </div>
                <div class="feature-text">
                    <h3>Fast Delivery</h3>
                    <p>Quick turnaround times so you can enjoy your photos sooner.</p>
                </div>
            </div>
            <div class="feature-item fade-in-left">
                <div class="feature-icon">
                    <i class="fas fa-smile"></i>
                </div>
                <div class="feature-text">
                    <h3>Friendly Service</h3>
                    <p>Comfortable, relaxed sessions that bring out your natural best.</p>
                </div>
            </div>
            <div class="feature-item fade-in-right">
                <div class="feature-icon">
                    <i class="fas fa-image"></i>
                </div>
                <div class="feature-text">
                    <h3>High Resolution Images</h3>
                    <p>Crisp, detailed photos perfect for printing and large displays.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section" id="contact">
        <div class="section-header fade-in">
            <h2>Get In Touch</h2>
            <p>Book your photography session today</p>
        </div>
        <div class="contact-grid">
            <div class="contact-info fade-in-left">
                <h3>Location</h3>
                <p class="address">
                    Nethum Photography<br>
                    Ihala Obadayagama<br>
                    Kalankuttiya<br>
                    Galnewa<br>
                    Sri Lanka
                </p>
                
                <h3>Contact</h3>
                <div class="contact-item">
                    <i class="fab fa-whatsapp"></i>
                    <div>
                        <strong>WhatsApp</strong>
                        <a href="https://wa.me/94773950975" target="_blank">077 395 0975</a>
                    </div>
                </div>
                
                <div class="contact-item">
                    <i class="fas fa-phone"></i>
                    <div>
                        <strong>Phone</strong>
                        <span>+94 77 395 0975</span>
                    </div>
                </div>
                
                <div class="contact-item">
                    <i class="fas fa-envelope"></i>
                    <div>
                        <strong>Email</strong>
                        <a href="mailto:info@nethumphotography.com">info@nethumphotography.com</a>
                    </div>
                </div>

                <a href="https://wa.me/94773950975" target="_